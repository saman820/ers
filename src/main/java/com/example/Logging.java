package com.example;

import org.apache.log4j.Logger;

public class Logging {
	public final Logger log = Logger.getLogger(MainDriver.class); 
//	this.log.setLevel(Level.INFO);
//	MainDriver.log.debug("after deposit/withdraw");
//	MainDriver.log.error(e.getMessage());
//	new Logging().log.debug("in Dao");
//	new Logging().log.error(e.getMessage());
}
